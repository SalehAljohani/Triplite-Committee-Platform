@model List<Triplite_Committee_Platform.ViewModels.SearchViewModel>
@{
    ViewData["Title"] = "Search";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (TempData["Message"] != null)
{
    <div class="alert alert-danger" role="alert">
        @TempData["Message"]
    </div>
}

<div class="card p-3 w-75 mt-5 mx-auto rounded-4">
    <div class="row">
        <strong class="fs-3">Search</strong>
    </div>
    <div class="card-body">
        <form asp-action="Index" method="post">
            <div class="form-group row justify-content-center">
                <div class="col-sm-5">
                    <input type="text" class="form-control" name="search" placeholder="Enter search term" />
                </div>
                @if (ViewBag.Colleges != null)
                {
                    <div class="col-sm-3">
                        <select class="form-control" asp-items="ViewBag.Colleges" name="SelectedCollege" onchange="enableDepartmentSelection(this)">
                            <option value="">Select College</option>
                        </select>
                    </div>
                }
                @if (ViewBag.Departments != null)
                {
                    <div class="col-sm-3">
                        <select class="form-control" name="SelectedDepartment" disabled>
                            <option value="">Select Department</option>
                        </select>
                    </div>
                }
                <div class="col-sm-1">
                    <button type="submit" class="btn bg-opacity-10 btn-md " data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="Search" id=""><i class="fa fa-search fs-4 text-primary  " title=" Search"></i> </button>
                </div>
            </div>
        </form>
    </div>
</div>


<div class="card p-4 w-75 mt-2 mx-auto rounded-4">
    <table class="table align-middle bg-white table-striped w-100" id="DTable">
        <thead>
            <tr>
                <th class="text-center">#</th>
                <th class="text-center">Name</th>
                <th class="text-center">Type</th>
                <th class="text-center">Status</th>
                @if (ViewBag.ActiveRole == "Admin")
                {
                    <th class="text-center">College</th>
                    <th class="text-center">Department</th>
                }
                else if (ViewBag.ActiveRole == "College Dean" || ViewBag.ActiveRole == "Vice Dean")
                {
                    <th class="text-center">Department</th>
                }
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td class="text-center">1</td>
                <td class="text-center">Saleh Aljohani</td>
                <td class="text-center">Extend</td>
                <td class="mb-0 text-center"><p class="badge text-bg-success fs-6">Completed</p></td>
                @if (ViewBag.ActiveRole == "Admin")
                {
                    <td class="text-center">CSE</td>
                    <td class="text-center">information system</td>

                }
                else if (ViewBag.ActiveRole == "College Dean" || ViewBag.ActiveRole == "Vice Dean")
                {
                    <td class="text-center">information system</td>

                }
                <td>
                    <div class="text-center">
                        <a asp-action="" class="btn btn-md" data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="View Details" id="iconhover"><i class="fa-solid fa-circle-info fs-4 text-secondary "></i></a>
                        <a asp-action="" class="btn btn-md" data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="Print" id="iconhover"><i class="fa-solid fa-print fs-4 text-primary"></i></a>
                    </div>
                </td>
            </tr>
            <tr>
                <td class="text-center">1</td>
                <td class="text-center">Saleh Aljohani</td>
                <td class="text-center">Extend</td>
                <td class="mb-0 text-center"><p class="badge text-bg-success fs-6">Completed</p></td>
                @if (ViewBag.ActiveRole == "Admin")
                {
                    <td class="text-center">CSE</td>
                    <td class="text-center">information system</td>

                }
                else if (ViewBag.ActiveRole == "College Dean" || ViewBag.ActiveRole == "Vice Dean")
                {
                    <td class="text-center">information system</td>

                }
                <td>
                    <div class="text-center">
                        <a asp-action="" class="btn btn-md" data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="View Details" id="iconhover"><i class="fa-solid fa-circle-info fs-4 text-secondary "></i></a>
                        <a asp-action="" class="btn btn-md" data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="Print" id="iconhover"><i class="fa-solid fa-print fs-4 text-primary"></i></a>
                    </div>
                </td>
            </tr>
            <tr>
                <td class="text-center">1</td>
                <td class="text-center">Saleh Aljohani</td>
                <td class="text-center">Extend</td>
                <td class="mb-0 text-center"><p class="badge text-bg-success fs-6">Completed</p></td>
                @if (ViewBag.ActiveRole == "Admin")
                {
                    <td class="text-center">CSE</td>
                    <td class="text-center">information system</td>

                }
                else if (ViewBag.ActiveRole == "College Dean" || ViewBag.ActiveRole == "Vice Dean")
                {
                    <td class="text-center">information system</td>

                }
                <td>
                    <div class="text-center ">
                        <a asp-action="" class="btn btn-md" data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="View Details" id="iconhover"><i class="fa-solid fa-circle-info fs-4 text-secondary "></i></a>
                        <a asp-action="" class="btn btn-md" data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="Print" id="iconhover"><i class="fa-solid fa-print fs-4 text-primary"></i></a>
                    </div>
                </td>
            </tr>
        </tbody>
    </table>
    @*End of test table*@

    @if (Model != null && Model.Count > 0)
    {
        <hr />
        <table class="table table-striped" id="DTable">
            <thead>
                <tr>
                    <th>Board Number</th>
                    <th>Scholarship Name</th>
                    <th>Board Type</th>
                    <th>Board Date</th>
                    <th>Board Status</th>
                    @if (ViewBag.ActiveRole == "Admin")
                    {
                        <th>Scholarship College</th>
                        <th>Scholarship Department</th>
                    }
                    else if (ViewBag.ActiveRole == "College Dean" || ViewBag.ActiveRole == "Vice Dean")
                    {
                        <th>Scholarship Department</th>
                    }
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        @foreach (var board in item.Boards)
                        {
                            <td>@board.BoardNo</td>
                            <td>@item.Name</td>
                            <td>@board.RequestType.RequestTypeName</td>
                            <td>@board.ReqDate</td>
                            <td class="mb-0"><p class="@((board.ReqStatus == "Completed") ? "text-bg-success" :"text-bg-danger")">@board.ReqStatus</p></td>
                            @if (ViewBag.ActiveRole == "Admin")
                            {
                                <td>@item?.CollegeName</td>
                                <td>@item?.DeptName</td>
                            }
                            else if (ViewBag.ActiveRole == "College Dean" || ViewBag.ActiveRole == "Vice Dean")
                            {
                                <td>@item?.DeptName</td>
                            }
                        }
                        <td>
                            <div class="d-flex ">
                                <a asp-action="" class="btn btn-md" data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="View Details" id="iconhover"><i class="fa-solid fa-circle-info fs-4 text-secondary "></i></a>
                                <a asp-action="" class="btn btn-md" data-bs-custom-class="custom-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="Print" id="iconhover"><i class="fa-solid fa-print fs-4 text-primary"></i></a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>

@* DataTable Library used to add filters to the tables *@
<script src="https://cdn.jsdelivr.net/npm/jquery@2.2.4/dist/jquery.min.js"></script>
<link rel="stylesheet" href="https://cdn.datatables.net/2.0.5/css/dataTables.bootstrap5.min.css" />
<script src="https://cdn.datatables.net/2.0.5/js/dataTables.min.js"></script>
<script src="https://cdn.datatables.net/2.0.5/js/dataTables.bootstrap5.min.js"></script>

<script>
    $(document).ready(function () {
        $('#DTable').DataTable({

        });
    });

    var allDepartments = JSON.parse('@Html.Raw(ViewData["Departments"])');

    function enableDepartmentSelection(selectElement) {
        var departmentSelect = document.getElementsByName("SelectedDepartment")[0];
        var collegeId = selectElement.value;

        departmentSelect.disabled = collegeId === "";
        departmentSelect.innerHTML = '<option value=""> Select Department </option>';

        if (collegeId !== "") {
            allDepartments.forEach(function (department) {
                if (department.CollegeNo.toString() === collegeId.toString()) {
                    let option = new Option(department.DeptName, department.DeptNo);
                    departmentSelect.add(option);
                }
            });
            departmentSelect.disabled = departmentSelect.options.length <= 1;
        }
    }


    var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
    var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
        return new bootstrap.Tooltip(tooltipTriggerEl)
    });

</script>